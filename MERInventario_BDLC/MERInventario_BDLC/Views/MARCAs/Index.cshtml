@model IEnumerable<MERInventario_BDLC.Models.MARCA>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MAR_NOMBRE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MAR_NOMBRE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MAR_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MAR_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MAR_ID })
        </td>
    </tr>
}

</table>
